$('#more-user').remove();
$('#user-list').append("<%= j render partial: 'users/user_one', locals: {users: @users} %>")
$('#user-list').append("<%= j link_to_next_page(@users, 'もっと見る', id: 'more-user', remote: true) %>")
